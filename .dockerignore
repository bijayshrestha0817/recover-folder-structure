# version control & CI
.git
.gitignore
.github

# python caches / build artifacts
__pycache__
*.py[cod]
*.egg-info
.venv
venv

# tooling caches
.ruff_cache
.mypy_cache
.pytest_cache

# local data & secrets
db.sqlite3
.env
.env.*

# editor / agent / local-only
.vscode
.idea
.claude
dev

# docs & docker meta (not needed inside the image)
*.md
Dockerfile
docker-compose.yml
.dockerignore
Makefile
